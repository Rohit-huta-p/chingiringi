# UI Polish — Mockup-to-Code Alignment

> **Branch:** `v2-live-feat`
> **Goal:** Every built screen must match its mockup spec exactly.
> **Rule:** `git commit --no-verify` on every commit. Never touch `main`.

---

## Theme constants (NEVER use raw hex — always import from theme)

```ts
// chingiring-app/src/constants/theme.ts
Colors.primary        = '#4784E2'   // buyer blue
Colors.primaryLight   = '#8BB9FD'
Colors.primaryLight10 = '#E9F4FF'
Colors.orange         = '#F97316'   // seller orange
Colors.navy           = '#0C1A3D'
Colors.background     = '#F0F4F8'
Colors.backgroundGrey = '#F3F5F7'
Colors.text           = '#1e293b'
Colors.textSecondary  = '#64748b'
Colors.textWhite      = '#fff'

Fonts.regular / .medium / .semiBold / .bold / .extraBold
```

---

## File ownership (zero overlap between batches)

| Batch A | Batch B | Batch C |
|---------|---------|---------|
| `Buyer/LiveDiscoveryScreen.tsx` | `Seller/BusinessOnboardingScreen.tsx` | `Live/BroadcasterScreen.tsx` |
| `Buyer/BuyerOnboardingScreen.tsx` | `Seller/SellerDashboardScreen.tsx` | `Live/ViewerScreen.tsx` |
| `Buyer/BuyerProfileScreen.tsx` | `Seller/MyStoreScreen.tsx` | |
| `Dashboard/OfflineStoresScreen.tsx` | `Seller/GoLiveTabScreen.tsx` | |
| `Dashboard/StoreDetailScreen.tsx` | `Seller/SellerProfileScreen.tsx` | |
| `Auth/RoleSelectionScreen.tsx` | `Seller/StoreVerificationScreen.tsx` | |
| `Auth/MobileLoginScreen.tsx` | `components/ProductFormModal.tsx` | |
| `Auth/MobileOTPScreen.tsx` | | |

---

## ═══════════════════════════════════════════════════
## BATCH A PROMPT — Tab 1 (Buyer-side + Auth, 9 screens)
## ═══════════════════════════════════════════════════

Paste into a **fresh Claude Code terminal tab**:

---

```
You are updating the Chingiringi React Native app so every screen matches its design spec exactly.

Project root: /Users/rohithutagonna/Documents/Rohit/Chingiringi
Branch: v2-live-feat  ← verify before touching any file
Commit rule: git commit --no-verify  ← mandatory, skips slow graphify hook

RULES:
- Never use raw hex colours. Always import Colors + Fonts from ../../constants/theme.
- Never edit backend files or files listed under Batch B / C.
- Commit after every 3 screens with --no-verify.
- Each screen spec below is the source of truth — implement it exactly.

─────────────────────────────────────────────
SCREEN 1 of 9 — Live Discovery
File: chingiring-app/src/screens/Buyer/LiveDiscoveryScreen.tsx
─────────────────────────────────────────────
LAYOUT (top → bottom):
  • SafeAreaView, bg Colors.background
  • Toggle pill row (centered, gap 8): "Live" pill | "Stores" pill
      - Active pill: bg Colors.primary, text white, borderRadius 20
      - Inactive pill: bg white, text Colors.textSecondary, border Colors.primaryLight
      - Tapping "Stores" navigates to OfflineStoresScreen (or passes callback to parent)
  • FlatList of StreamCards, numColumns=2 on wide screens (>480px), 1 col on narrow
  • Pull-to-refresh (RefreshControl, Colors.primary tint)

STREAM CARD (each item):
  • borderRadius 12, overflow hidden, bg Colors.navy, shadow small
  • Thumbnail area (height 140): dark bg gradient overlay at bottom
      - Top-left: red "● LIVE" pill (bg #EF4444, text white, fontSize 11, bold)
      - Bottom-left: "👁 {viewerCount}" in white, fontSize 12
  • Below thumbnail (bg white, padding 10):
      - Store name: Fonts.semiBold, fontSize 13, Colors.text, 1 line
      - Stream title: Fonts.regular, fontSize 12, Colors.textSecondary, 2 lines
      - Follow button (right-aligned): see follow button spec below

FOLLOW BUTTON (3 states — use across all screens consistently):
  (a) Not following: outlined button, borderColor Colors.primary, text "Follow",
      color Colors.primary, icon "+" left, fontSize 12, paddingH 12, paddingV 5, borderRadius 16
  (b) Following: filled button, bg Colors.primary, text "Following",
      color white, icon "✓" left, same sizing
  (c) Loading: same size, ActivityIndicator inside, disabled

EMPTY STATE (no live streams):
  • Centered: camera SVG icon (40px, Colors.textSecondary), gap 12
  • "No live streams right now" — Fonts.semiBold, 16, Colors.text
  • "Follow stores to get notified when they go live" — Fonts.regular, 13, Colors.textSecondary
  • No CTA button

SOCKET: existing useSocket hook wires viewer count updates — keep that logic, just fix UI.

─────────────────────────────────────────────
SCREEN 2 of 9 — Store List  (Stores toggle)
File: chingiring-app/src/screens/Dashboard/OfflineStoresScreen.tsx
─────────────────────────────────────────────
LAYOUT:
  • Same Live/Stores toggle pills at top (exact same component as Screen 1)
  • Search bar below pills: bg white, borderRadius 10, placeholder "Search stores…",
    magnify icon left, Colors.textSecondary placeholder
  • Category filter chips horizontal scroll (no scrollbar):
    "All" | "Fashion" | "Electronics" | "Grocery" | "Food & Cafe" | "Health" | "Jewellery" | "Sports" | "Beauty"
    Active chip: bg Colors.primary, text white; inactive: bg white, text Colors.textSecondary, border #E2E8F0
  • FlatList of StoreCards (single column list)
  • Pull-to-refresh

STORE CARD:
  • bg white, borderRadius 12, padding 14, marginBottom 10, shadow small
  • Row layout:
      Left: logo circle (48px, bg Colors.primaryLight10, initials in Colors.primary, Fonts.bold)
      Right (flex):
        Row 1: store name (Fonts.semiBold 14, Colors.text) + open/closed badge right-aligned
          Open badge: bg #DCFCE7, text #16A34A, "Open", fontSize 11
          Closed badge: bg #FEE2E2, text #DC2626, "Closed"
        Row 2: category chip (bg Colors.backgroundGrey, Colors.textSecondary, 11px) + area text
        Row 3: "👥 {followerCount} followers" in Colors.textSecondary, fontSize 12

EMPTY STATE:
  • Map-pin icon (40px, Colors.textSecondary), gap 12
  • "No stores found" — Fonts.semiBold, 16
  • "Try a different category or search term" — Fonts.regular, 13, Colors.textSecondary

─────────────────────────────────────────────
SCREEN 3 of 9 — Store Profile (public)
File: chingiring-app/src/screens/Dashboard/StoreDetailScreen.tsx
─────────────────────────────────────────────
LAYOUT:
  • ScrollView, bg Colors.background
  • HERO: Image height 180 (bg Colors.navy if no image), borderRadius 0
  • LOGO: circle 72px, positioned overlapping bottom of hero (translateY -36),
    bg Colors.primaryLight10, initials Fonts.bold 22 Colors.primary, border 3 white
  • "LIVE NOW" banner (if store.isLive): bg #EF4444, text white, Fonts.semiBold 12,
    full-width, padding 6, textAlign center — sits between hero and content
  • CONTENT CARD (bg white, borderRadius 16, marginTop -16, padding 20):
      - Store name: Fonts.extraBold 20, Colors.navy
      - Verified badge (if isVerified): blue tick icon + "Verified", Colors.primary, fontSize 12
      - Category chip + area text row (Colors.textSecondary, 13)
      - Stats row: "{followerCount} Followers" | "{totalStreams} Streams" (Fonts.semiBold 14)
      - Follow button (full-width, height 44, borderRadius 10) — 3 states per follow spec above
  • TABS: "Products" | "About" — pill tab bar, bg Colors.backgroundGrey, active bg white
  • Products tab: 2-col FlatList of ProductMiniCards
      Each card: image 80px, name 2-line truncate, "₹{price}" Fonts.semiBold Colors.primary
  • About tab: list of rows with icon + label
      🕐 "{openTime} – {closeTime}"  |  📍 "{address}, {area}"  |  📞 "{phone}"
      🗺 "View on Maps" → Linking.openURL(mapsUrl)

─────────────────────────────────────────────
SCREEN 4 of 9 — Role Selection
File: chingiring-app/src/screens/Auth/RoleSelectionScreen.tsx
─────────────────────────────────────────────
LAYOUT:
  • SafeAreaView, bg Colors.background, flex 1, centered
  • Title: "How will you use Chingiringi?" — Fonts.extraBold 26, Colors.text, textAlign center
  • Subtitle: "Choose your role — you can change it later" — Fonts.regular 15, Colors.textSecondary
  • Gap 40, then two cards stacked vertically (gap 16):

BUYER CARD:
  • bg Colors.primaryLight10, border 2 Colors.primaryLight, borderRadius 20, padding 28
  • Centered vertically:
      - Icon circle 72px, bg #DBEAFE, ShoppingBag icon 36px, Colors.primary
      - "I'm a Buyer" — Fonts.bold 20, Colors.primary
      - "Discover live deals, follow your favourite stores, and earn cashback." — Fonts.regular 14, Colors.textSecondary, centered

SELLER CARD:
  • bg #FFF7ED, border 2 #FDBA74, borderRadius 20, padding 28
  • Centered vertically:
      - Icon circle 72px, bg #FFEDD5, Store icon 36px, Colors.orange
      - "I'm a Seller" — Fonts.bold 20, Colors.orange
      - "Go live, manage your store, and grow your customer base." — Fonts.regular 14, Colors.textSecondary, centered

LOADING state: ActivityIndicator replaces card content, matches accent color

─────────────────────────────────────────────
SCREEN 5 of 9 — Buyer Onboarding
File: chingiring-app/src/screens/Buyer/BuyerOnboardingScreen.tsx
─────────────────────────────────────────────
STEP 1 — Location:
  • Progress: 2 dots at top, first filled Colors.primary, second empty
  • Large MapPin icon (80px, Colors.primary) centered with 32px gap below
  • "Allow location access" — Fonts.extraBold 24, Colors.text, centered
  • "We'll show you live streams and stores nearby" — Fonts.regular 15, Colors.textSecondary, centered
  • Gap 40
  • "Allow Location" — primary blue button, full width, height 52, borderRadius 12
  • "Skip for now" — ghost link, Colors.textSecondary, Fonts.medium 14, marginTop 16, centered

STEP 2 — Categories:
  • Progress: both dots filled
  • "What are you interested in?" — Fonts.extraBold 22, Colors.text
  • "Pick categories to personalise your feed" — Fonts.regular 14, Colors.textSecondary
  • Wrap grid of category chips (flexWrap wrap, flexDirection row, gap 10):
    Each chip: emoji icon + label, bg white when unselected / bg Colors.primary text white when selected
    borderRadius 20, padding 10 16, Fonts.medium 14, border Colors.primaryLight
    Categories: 🛍 Fashion | 📱 Electronics | 🥑 Grocery | ☕ Food & Cafe | 💊 Health | 💍 Jewellery | ⚽ Sports | 💄 Beauty
  • "Continue" — primary blue button, full width, height 52, borderRadius 12, bottom fixed

─────────────────────────────────────────────
SCREEN 6 of 9 — Buyer Profile
File: chingiring-app/src/screens/Buyer/BuyerProfileScreen.tsx
─────────────────────────────────────────────
LAYOUT:
  • ScrollView, bg Colors.background
  • HEADER CARD (bg Colors.navy, padding 24, borderRadius 0):
      - Avatar circle 72px: profile image or initials (Fonts.extraBold 28, white)
      - Name: Fonts.extraBold 20, white
      - Email: Fonts.regular 13, rgba(255,255,255,0.6)
      - "Edit Profile" outlined button (border white, text white, small, borderRadius 20, right side)
  • STATS ROW (bg white, 3 tiles):
      Each tile: number (Fonts.extraBold 20, Colors.primary) + label (Fonts.regular 12, Colors.textSecondary)
      "Stores Following" | "Streams Watched" | "Cashback Earned"
      Dividers between tiles (1px Colors.backgroundGrey)
  • FOLLOWING SECTION:
      - "Following" label Fonts.semiBold 14, Colors.text, padding 16
      - Horizontal ScrollView of store chips:
          Each chip: logo circle 48px + store name below (Fonts.medium 11, Colors.text, max 2 lines)
          spacing 12
      - Empty state if none: "Follow stores to see them here" — Colors.textSecondary, centered
  • SETTINGS LINK row: gear icon + "Settings" + chevron, Colors.text, Fonts.medium 14

─────────────────────────────────────────────
SCREEN 7 of 9 — Auth (Mobile Login + OTP)
Files: chingiring-app/src/screens/Auth/MobileLoginScreen.tsx
       chingiring-app/src/screens/Auth/MobileOTPScreen.tsx
─────────────────────────────────────────────
LOGIN SCREEN:
  • bg white, SafeAreaView, centered vertically with ScrollView
  • Logo (app icon or ChingiRingi wordmark) — centered, 80px height, marginBottom 32
  • "Sign in to Chingiringi" — Fonts.extraBold 26, Colors.navy, centered
  • "Welcome back! 👋" — Fonts.regular 14, Colors.textSecondary, centered, marginBottom 32
  • Google button: bg white, border 1 #E2E8F0, borderRadius 12, height 52, full width
      Row: Google 'G' logo (20px) + "Continue with Google" — Fonts.semiBold 15, Colors.text
  • Divider row: line + "or" + line — Colors.backgroundGrey lines, Colors.textSecondary "or"
  • Tab row: "Phone" | "Email" — Fonts.semiBold 14, active underline Colors.primary
  • Input: bg Colors.backgroundGrey, borderRadius 12, height 52, Fonts.regular 15
      Phone tab: "+91" prefix, numeric keyboard
      Email tab: standard email keyboard
  • "Continue" — full width, height 52, bg Colors.primary, text white, Fonts.semiBold 16, borderRadius 12

OTP SCREEN:
  • bg white, SafeAreaView
  • Back arrow top-left
  • "Enter OTP" — Fonts.extraBold 24, Colors.navy
  • "Sent to {phoneOrEmail}" — Fonts.regular 14, Colors.textSecondary
  • 6 digit boxes (gap 10, each: width 48, height 56, bg Colors.backgroundGrey,
    borderRadius 12, Fonts.extraBold 22, Colors.navy, border 2 when focused Colors.primary)
  • "Verify" — full width, height 52, bg Colors.primary, text white, Fonts.semiBold 16, borderRadius 12
  • Resend row: "Didn't get it? " + "Resend ({countdown}s)" or "Resend" active link Colors.primary

─────────────────────────────────────────────
SCREEN 8 of 9 — Follow States (component polish)
Files: chingiring-app/src/screens/Dashboard/StoreDetailScreen.tsx
       chingiring-app/src/screens/Buyer/LiveDiscoveryScreen.tsx
─────────────────────────────────────────────
Ensure both files use this exact pattern for follow buttons (extract to inline helper if needed):

  NOT FOLLOWING:
    borderWidth 1.5, borderColor Colors.primary, borderRadius 16
    text "Follow", color Colors.primary, Fonts.semiBold 13
    "+" icon left (Feather/Plus, size 14, Colors.primary)
    paddingH 14, paddingV 6

  FOLLOWING:
    bg Colors.primary, borderRadius 16
    text "Following", color white, Fonts.semiBold 13
    "✓" icon left (Feather/Check, size 14, white)
    same padding

  LOADING:
    same dimensions as above, ActivityIndicator size="small" color=Colors.primary, disabled

─────────────────────────────────────────────
SCREEN 9 of 9 — Empty States
Files: chingiring-app/src/screens/Buyer/LiveDiscoveryScreen.tsx
       chingiring-app/src/screens/Dashboard/OfflineStoresScreen.tsx
─────────────────────────────────────────────
Standard empty state pattern (reuse across both):
  • Outer: flex 1, alignItems center, justifyContent center, paddingV 60
  • Icon: Feather icon, size 56, color Colors.textSecondary, marginBottom 16
  • Heading: Fonts.semiBold 17, Colors.text, textAlign center, marginBottom 8
  • Subtitle: Fonts.regular 14, Colors.textSecondary, textAlign center, maxWidth 260
  • CTA button (optional): outlined, Colors.primary, borderRadius 20, paddingH 20 paddingV 10, Fonts.semiBold 14, marginTop 20

LiveDiscovery empty: icon="video" heading="No live streams right now" sub="Follow stores to get notified"
OfflineStores empty: icon="map-pin" heading="No stores found" sub="Try a different search or category"

═══════════════════════════════════════════════════
INTERMEDIATE COMMIT (after screens 1-3):
git add -A && git commit --no-verify -m "feat(ui): Batch A pt1 — LiveDiscovery, OfflineStores, StoreDetail match mockups

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

INTERMEDIATE COMMIT (after screens 4-6):
git add -A && git commit --no-verify -m "feat(ui): Batch A pt2 — RoleSelection, BuyerOnboarding, BuyerProfile match mockups

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

FINAL COMMIT (after screens 7-9):
git add -A && git commit --no-verify -m "feat(ui): Batch A pt3 — Auth, follow states, empty states match mockups

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

Then push:
git push --no-verify origin v2-live-feat
```

---

## ═══════════════════════════════════════════════════
## BATCH B PROMPT — Tab 2 (Seller-side, 9 screens)
## ═══════════════════════════════════════════════════

Paste into a **second fresh Claude Code terminal tab**:

---

```
You are updating the Chingiringi React Native app so every seller screen matches its design spec.

Project root: /Users/rohithutagonna/Documents/Rohit/Chingiringi
Branch: v2-live-feat  ← verify before touching any file
Commit rule: git commit --no-verify  ← mandatory

RULES:
- Never use raw hex. Import Colors + Fonts from ../../constants/theme.
- Never edit backend files.
- Do NOT touch Batch A files: Buyer/, Auth/, OfflineStoresScreen, StoreDetailScreen.
- Seller accent colour is Colors.orange (#F97316). Use it for all primary CTAs.
- Commit after every 3 screens.

─────────────────────────────────────────────
SCREEN 1 of 9 — Business Onboarding Wizard
File: chingiring-app/src/screens/Seller/BusinessOnboardingScreen.tsx
─────────────────────────────────────────────
STRUCTURE: 4-step wizard (single screen, step state)

HEADER (sticky):
  • bg white, borderBottom 1 Colors.backgroundGrey
  • Progress bar: thin (4px) orange fill, borderRadius 2, width = (step/4)*100%
  • Step labels row: "Details" | "Location" | "Media" | "Review"
    Active: Fonts.semiBold 13, Colors.orange; inactive: Fonts.regular 13, Colors.textSecondary

STEP 1 — Store Details:
  • Card: bg white, borderRadius 16, padding 20, shadow small
  • "Store Name" label + TextInput (bg Colors.backgroundGrey, borderRadius 10, height 48)
  • "Short Name" label + TextInput (shown in compact displays, auto-suggests first 2 words)
  • "Category" label + category picker (row of chips, same pattern as buyer onboarding chips but orange active)

STEP 2 — Location:
  • "Address" TextInput (multiline, minHeight 80)
  • "Area / Neighbourhood" TextInput
  • "City" TextInput (default "Bengaluru")

STEP 3 — Media:
  • "Logo URL" label + TextInput + thumbnail preview (80px circle) if URL entered
  • "Phone" TextInput, phone keyboard

STEP 4 — Review & Submit:
  • Summary card listing all fields entered
  • "Submit" orange button (full width, height 52, bg Colors.orange, text white)

NAV: "Back" ghost link left, "Next →" orange button right (disabled if required field empty)

─────────────────────────────────────────────
SCREEN 2 of 9 — Seller Dashboard
File: chingiring-app/src/screens/Seller/SellerDashboardScreen.tsx
─────────────────────────────────────────────
LAYOUT (ScrollView, bg Colors.background):

GREETING HEADER (bg Colors.navy, padding 24):
  • "Good morning, {firstName}! 👋" — Fonts.extraBold 22, white
  • Date row: Fonts.regular 13, rgba(white,0.6)

VERIFICATION BANNER (if verificationStatus !== 'verified'):
  • bg #FEF9C3, border #FDE047, borderRadius 10, padding 12, margin 16
  • "⚠️ Your store isn't verified yet — go live capability is locked"
  • "Verify now →" link, Colors.orange, Fonts.semiBold 13, right side

2×2 METRIC TILES (gap 12, margin 16):
  Each tile: bg white, borderRadius 14, padding 18, shadow small
  • Icon (32px, Colors.orange) top-left
  • Number: Fonts.extraBold 28, Colors.navy
  • Label: Fonts.regular 12, Colors.textSecondary
  Tiles: Users2 "Followers" | Video "Streams" | Eye "Views (7d)" | Package "Products"

GO LIVE CTA CARD (margin 16):
  • bg Colors.orange, borderRadius 14, padding 20
  • Row: Camera icon (white, 32px) left + text block
      "Ready to go live?" — Fonts.extraBold 18, white
      "Start a stream and connect with buyers" — Fonts.regular 13, rgba(white,0.8)
  • "Start Stream →" small outlined white button right side

RECENT STREAMS (margin 16):
  • "Recent Streams" — Fonts.semiBold 14, Colors.text, marginBottom 10
  • List of up to 3 stream rows (bg white, borderRadius 10, padding 14):
      Title: Fonts.semiBold 14, Colors.text
      Row: date | 👁 viewers | ⏱ duration — Fonts.regular 12, Colors.textSecondary

─────────────────────────────────────────────
SCREEN 3 of 9 — My Store
File: chingiring-app/src/screens/Seller/MyStoreScreen.tsx
─────────────────────────────────────────────
LAYOUT (bg Colors.background):

STORE HEADER CARD (bg white, padding 20):
  • Row: logo circle 60px (initials, bg Colors.orange opacity 15%, text Colors.orange) +
         name column (Fonts.extraBold 18, Colors.navy) + verified badge
  • Category chip (bg Colors.backgroundGrey) + area text
  • Stats row: "{count} Products" | "{count} Followers" | "{count} Streams" separated by · (Fonts.semiBold 14)

FAB: + button bottom-right, size 56, bg Colors.orange, shadow, opens ProductFormModal

PRODUCT GRID (FlatList numColumns=2, gap 12, padding 12):
  Each product card (bg white, borderRadius 12, overflow hidden):
    • Image area height 120 (bg Colors.backgroundGrey if no image)
    • Stock badge top-right: bg Colors.orange text white "In Stock" / bg #E2E8F0 "Out of Stock"
    • Bottom padding 10:
        Name: Fonts.semiBold 13, Colors.text, 2-line max
        "₹{price}" — Fonts.bold 15, Colors.primary
    • Edit/Delete icons overlay bottom-right (when isOwner): pencil + trash, 20px, white bg circle

EMPTY STATE:
  • Package icon 56px Colors.textSecondary
  • "Add your first product" — Fonts.semiBold 17, Colors.text
  • "Showcase what you sell during live streams" — Fonts.regular 14, Colors.textSecondary
  • "Add Product" — orange outlined button

─────────────────────────────────────────────
SCREEN 4 of 9 — Go Live Tab
File: chingiring-app/src/screens/Seller/GoLiveTabScreen.tsx
─────────────────────────────────────────────
LAYOUT (ScrollView, bg Colors.background):

STATE A — Store not verified:
  • Banner (bg #FEF9C3, borderRadius 10, margin 16, padding 16):
      "⚠️ Verify your store to unlock live streaming"
      "Submit your documents →" — Colors.orange link

  Skip the hero section; show banner instead.

STATE B — Verified:
  • Hero section (bg white, borderRadius 16, margin 16, padding 32, centered):
      Camera icon illustration (48px, Colors.orange, inside circle bg Colors.orange opacity 10%)
      "You're ready to go live!" — Fonts.extraBold 22, Colors.navy
      "Connect with your buyers in real time" — Fonts.regular 14, Colors.textSecondary, marginBottom 24
      "▶ Start Live Stream" — full-width, height 52, bg Colors.orange, text white, Fonts.semiBold 16, borderRadius 12

TIPS SECTION (margin 16):
  "Tips for a great stream" — Fonts.semiBold 14, Colors.text
  3 tip cards (bg white, borderRadius 10, padding 14, marginBottom 8):
    💡 "Good lighting makes a big difference"
    📦 "Feature your bestsellers first"
    💬 "Engage with comments regularly"
    Each: icon 20px + body text Fonts.regular 13 Colors.text

PAST STREAMS LIST (margin 16):
  "Previous Streams" — Fonts.semiBold 14, Colors.text
  Each row: title (Fonts.semiBold 14) + date/viewers/duration row (Fonts.regular 12, Colors.textSecondary)
  + "Replay" chip (bg Colors.backgroundGrey, Fonts.medium 11)

─────────────────────────────────────────────
SCREEN 5 of 9 — Go Live Modal (bottom sheet)
File: chingiring-app/src/screens/Seller/GoLiveTabScreen.tsx  (modal within)
─────────────────────────────────────────────
TRIGGER: Tapping "Start Live Stream" button opens this Modal/BottomSheet.

SHEET STRUCTURE:
  • Handle bar: 4×36px, bg #D1D5DB, borderRadius 2, centered, marginTop 12
  • "Start a Live Stream" — Fonts.extraBold 20, Colors.navy, padding 20
  • FIELDS:
      "Stream Title" TextInput — bg Colors.backgroundGrey, borderRadius 10, height 48, Fonts.regular 15
      "Category" label + pill chips row (same categories as store, orange active)
      "Feature Products" label + horizontal scroll of product chips from seller's store
          Each chip: product image 32px circle + name truncated, bg Colors.backgroundGrey
          Selected: border Colors.orange, bg Colors.orange opacity 10%
  • Bottom: "▶ Go Live Now" — full width, height 56, bg Colors.orange, text white, Fonts.semiBold 16, borderRadius 14
  • Dismissible via swipe down or tapping outside

─────────────────────────────────────────────
SCREEN 6 of 9 — Product Form Modal
File: chingiring-app/src/components/ProductFormModal.tsx
─────────────────────────────────────────────
Modal (full-height sheet or centered modal):
  • "Add Product" / "Edit Product" — Fonts.extraBold 20, Colors.navy
  • FIELDS (each: label Fonts.semiBold 13 Colors.text, input bg Colors.backgroundGrey borderRadius 10):
      Product Name* — TextInput height 48
      Description — TextInput multiline, minHeight 80
      Price (₹)* — TextInput numeric keyboard, "₹" prefix
      Stock Quantity — TextInput numeric
      Image URL — TextInput + square thumbnail preview 80×80 (borderRadius 8) if URL present
  • BUTTONS row: "Cancel" ghost | "Save" bg Colors.orange text white borderRadius 10 height 44
  • Validation: name + price required, show inline error text Colors.red below field if empty on save

─────────────────────────────────────────────
SCREEN 7 of 9 — Seller Profile
File: chingiring-app/src/screens/Seller/SellerProfileScreen.tsx
─────────────────────────────────────────────
OWN PROFILE VIEW (when userId === currentUser.id):
  HEADER (bg Colors.navy, padding 24):
    • Avatar 72px circle, name Fonts.extraBold 20 white, "Store Owner" tag
    • "Verified ✓" badge if verified (bg Colors.orange opacity 20%, text Colors.orange)
    • "Edit Profile" outlined white button top-right
  STATS: same 3-tile pattern as buyer profile (Followers | Streams | Products) — Colors.orange numbers
  SETTINGS LINK: gear + "Settings" + chevron

PUBLIC VIEW (buyer visits seller profile):
  • Store hero image (height 160, bg Colors.navy)
  • Store name + verified badge overlapping bottom
  • Follow button (full-width, Colors.primary, 3 states per follow spec)
  • "Past Streams" section: list of 3 most recent
  • "Products" section: 2-col grid preview (first 4 products)

─────────────────────────────────────────────
SCREEN 8 of 9 — Store Verification (submit + pending + verified states)
File: chingiring-app/src/screens/Seller/StoreVerificationScreen.tsx
─────────────────────────────────────────────
All 4 states share: SafeAreaView, bg Colors.background, centered content card (bg white, borderRadius 16, padding 28, margin 16)

STATE: unverified
  • FileText icon 48px Colors.orange, marginBottom 20
  • "Verify Your Store" — Fonts.extraBold 22, Colors.navy
  • "Submit a document to unlock live streaming" — Fonts.regular 14, Colors.textSecondary
  • "Document Type" label + 3 chips: "GST Certificate" | "FSSAI Licence" | "Trade Licence"
      Active chip: bg Colors.orange text white; inactive: bg Colors.backgroundGrey
  • "Document URL" TextInput (bg Colors.backgroundGrey, borderRadius 10, height 48)
  • "Submit for Verification" — full width, height 52, bg Colors.orange, borderRadius 12

STATE: pending
  • Clock icon 48px #F59E0B (amber), marginBottom 20
  • "Under Review" — Fonts.extraBold 22, Colors.navy
  • "We'll review your document within 2–3 business days" — Fonts.regular 14, Colors.textSecondary
  • Submitted date row: Fonts.regular 13, Colors.textSecondary

STATE: verified
  • ShieldCheck icon 48px #16A34A (green), marginBottom 20
  • "Store Verified ✓" — Fonts.extraBold 22, #16A34A
  • "You can now go live and reach buyers" — Fonts.regular 14, Colors.textSecondary
  • Verified date row

─────────────────────────────────────────────
SCREEN 9 of 9 — Verification Rejected
File: chingiring-app/src/screens/Seller/StoreVerificationScreen.tsx (rejected state)
─────────────────────────────────────────────
STATE: rejected  (same card structure as above)
  • XCircle icon 48px #DC2626 (red), marginBottom 20
  • "Verification Rejected" — Fonts.extraBold 22, #DC2626
  • Rejection reason card: bg #FEF2F2, border #FECACA, borderRadius 10, padding 14
      "Reason: {verificationDoc.rejectionReason}" — Fonts.regular 14, #DC2626
  • "Resubmit Documents" — full width, height 52, border 2 Colors.orange, text Colors.orange,
    bg white, borderRadius 12  (outlined orange button)
  • Tapping Resubmit resets state back to unverified form above

═══════════════════════════════════════════════════
INTERMEDIATE COMMIT (after screens 1-3):
git add -A && git commit --no-verify -m "feat(ui): Batch B pt1 — BusinessOnboarding, SellerDashboard, MyStore match mockups

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

INTERMEDIATE COMMIT (after screens 4-6):
git add -A && git commit --no-verify -m "feat(ui): Batch B pt2 — GoLiveTab, GoLiveModal, ProductFormModal match mockups

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

FINAL COMMIT (after screens 7-9):
git add -A && git commit --no-verify -m "feat(ui): Batch B pt3 — SellerProfile, StoreVerification (all states) match mockups

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

Then push:
git push --no-verify origin v2-live-feat
```

---

## ═══════════════════════════════════════════════════
## BATCH C PROMPT — Tab 1 Day 2 (Live streaming, 3 screens)
## ═══════════════════════════════════════════════════

Run this the NEXT DAY in a fresh session:

```
You are updating the live-streaming screens in the Chingiringi app to match design specs.

Project root: /Users/rohithutagonna/Documents/Rohit/Chingiringi
Branch: v2-live-feat  ← verify first
Commit rule: git commit --no-verify

─────────────────────────────────────────────
SCREEN 1 of 3 — Broadcaster
File: chingiring-app/src/screens/Live/BroadcasterScreen.tsx
─────────────────────────────────────────────
Full-screen dark UI (bg #0A0A0A). No system chrome visible (StatusBar hidden).

TOP BAR (absolute top, paddingTop safeArea, paddingH 16):
  Left: "● LIVE" pill — bg #EF4444, text white, Fonts.bold 13, borderRadius 12, padding 4 10
        + " {viewerCount} watching" — Fonts.regular 13, white, marginLeft 8
        + duration timer "00:00" — Fonts.bold 14, white, marginLeft 8
  Right: X button — circle 36px, bg rgba(white,0.15), ✕ icon white 20px

RIGHT SIDEBAR (absolute right 16, center vertically, gap 20):
  Each icon: circle 44px bg rgba(white,0.15), icon white 22px
  Icons: FlipCamera | Mic / MicOff (toggle) | Share2

CAMERA AREA: flex 1, bg #111 (placeholder for expo-camera/Daily.co stub)
  Subtle gradient overlay at bottom (transparent → rgba(0,0,0,0.7), height 200)

BOTTOM OVERLAY (absolute bottom, paddingBottom safeArea, paddingH 12):
  • Chat feed: last 5 messages, each row:
      avatar circle 28px + "{name}: {message}" — Fonts.regular 13, white, bg rgba(0,0,0,0.4) borderRadius 8, padding 6 10
      FlatList inverted, maxHeight 160, no scroll indicator
  • Input row (marginTop 8, gap 8):
      TextInput: flex 1, bg rgba(white,0.15), borderRadius 20, height 40, color white, padding 8 14, Fonts.regular 14
      Send button: circle 40px bg Colors.primary, send icon white
      Heart button: circle 40px bg rgba(white,0.15), heart icon white — triggers heart animation

HEART ANIMATION: hearts (❤️) animate upward from bottom-right, random X offset, fade out, 1s duration

END STREAM CONFIRMATION (Modal when X tapped):
  • Semi-transparent dark overlay
  • Card: bg white, borderRadius 16, padding 24, margin 32
  • "End Stream?" — Fonts.extraBold 20, Colors.navy
  • "Your viewers will be disconnected" — Fonts.regular 14, Colors.textSecondary
  • Buttons row: "Cancel" ghost | "End Stream" bg #EF4444 text white

─────────────────────────────────────────────
SCREEN 2 of 3 — Viewer
File: chingiring-app/src/screens/Live/ViewerScreen.tsx
─────────────────────────────────────────────
Full-screen dark UI (bg #0A0A0A). No system chrome.

TOP BAR (absolute top, paddingTop safeArea, paddingH 16):
  Left: store avatar 36px circle + store name Fonts.semiBold 14 white (tappable → store profile)
        + " · " + "👁 {viewerCount}" Fonts.regular 13 rgba(white,0.7)
  Right: ✕ button circle 36px bg rgba(white,0.15)

VIDEO AREA: flex 1, bg #111 (Daily.co stub)
  Gradient overlay at bottom (transparent → rgba(0,0,0,0.8), height 260)

FEATURED PRODUCTS BAR (absolute, above input row, height 72):
  Horizontal ScrollView, padding 12
  Each product chip (bg rgba(white,0.15), borderRadius 10, padding 8 12, gap 8):
    Product image 40px circle + name 2-line max Fonts.regular 12 white + "₹{price}" Fonts.bold 12 Colors.primary
  Tapping → navigate to product detail

BOTTOM OVERLAY (absolute bottom, paddingBottom safeArea, paddingH 12):
  • Chat feed: same as broadcaster (last 5, inverted FlatList)
  • Input row: same layout (TextInput + Send + Heart)

HEARTS: same animation as broadcaster

─────────────────────────────────────────────
SCREEN 3 of 3 — Stream Ended (post-stream summary)
File: chingiring-app/src/screens/Live/BroadcasterScreen.tsx  (state after stream ends)
─────────────────────────────────────────────
When stream ends (after confirming End Stream), show this overlay ON TOP of the frozen camera frame:

  • Full overlay: bg rgba(0,0,0,0.85), flex 1, centered
  • Stats card: bg white, borderRadius 20, padding 28, marginH 32
      "Stream Ended" — Fonts.extraBold 24, Colors.navy, marginBottom 20
      4 stat rows (icon + label + value):
        ⏱ "Duration"   →  "{duration}"
        👁 "Peak Viewers" → "{peakViewers}"
        ❤️ "Hearts"     → "{heartsCount}"
        💬 "Messages"   → "{messagesCount}"
        Each row: icon 20px Colors.orange + Fonts.regular 14 Colors.textSecondary + Fonts.bold 16 Colors.navy right-aligned
        Dividers between rows (1px Colors.backgroundGrey)
  • Buttons row (gap 12, marginTop 24):
      "Share Replay" — border 2 Colors.orange, text Colors.orange, flex 1, height 48, borderRadius 12
      "Done" — bg Colors.orange, text white, flex 1, height 48, borderRadius 12

═══════════════════════════════════════════════════
COMMIT WHEN DONE:
git add -A && git commit --no-verify -m "feat(ui): Batch C — live streaming screens match mockups

Screens updated: BroadcasterScreen (live + end-stream summary), ViewerScreen.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

git push --no-verify origin v2-live-feat
```
